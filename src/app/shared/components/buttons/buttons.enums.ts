export enum ButtonSize {
  SMALL = 'small',
  MEDIUM = 'medium',
  BIG  = 'big'
}

export enum ButtonType {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  DEFAULT = 'default',
  CANCEL = 'cancel',
}

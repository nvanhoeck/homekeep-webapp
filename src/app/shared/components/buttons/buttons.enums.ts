export enum ButtonSize {
  SMALL = 'small',
  MEDIUM = 'medium',
  BIG  = 'big'
}

export enum ButtonType {
  SUCCESS = 'success',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  DEFAULT = 'default',
  CANCEL = 'cancel',
  ERROR = 'error',

  PRIMARY_BORDER_ONLY = 'primary_border_only',
  SECONDARY_BORDER_ONLY = 'secondary_border_only',
  DEFAULT_BORDER_ONLY = 'default_border_only',
  CANCEL_BORDER_ONLY = 'cancel_border_only',
  WHITE_BORDER_ONLY = 'white_border_only',
  ERROR_BORDER_ONLY = 'error_border_only',

  ERROR_BORDER_AND_TEXT = 'error_border_and_text',


}

export enum ButtonClass {
  TEXT = 'text',
  INPUT = 'input',
  PASSWORD = 'password',
  ICON = 'icon',
}
